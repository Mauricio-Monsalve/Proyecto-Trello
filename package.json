{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "shiny": "npx sass --watch style.scss:style.css"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.86.0",
    "vite": "^6.3.4"
  },
  "dependencies": {
    "extract-uri-image": "^1.1.0"
  }
}
